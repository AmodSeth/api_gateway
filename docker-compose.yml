version: '3'
services:
  api-gateway:
    build: .
    container_name: api_gateway_node_container
    ports:
      - '8112:8765'
    command: bash -c "npm install && ./start-server.sh"
    volumes:
      - ./:/application
    env_file: .env
    restart: always